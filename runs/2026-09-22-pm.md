# 2026-09-22 pm — ABORTED (no spend, nothing posted)

**Abort reason:** the sandbox's egress policy denies every third-party host, so the tour
could not be recorded. Aborted at **step 2 (dry run)**, before the one TopView spend.

## Slot
- Run started 11:45 UTC → `pm` slot. Voice would have been Michael - Narrator
  `konKRRWBWSO4ybjaMm9oFUkWloz6ROiz`. Posts were due 12:30 UTC (18:00 IST).

## Step 0 — set up (passed)
- `git pull --rebase`: already up to date. `covered.json` holds one entry
  (the hand-made Claude demo, 2026-09-22) — no conflict with today's pick.
- TopView credit **before: 2439.05**; **after: 2439.05** → **spend 0.0 credits**.
- Buffer queue (org `6aa7c7f3ac8fd4ea0a97166a`): Instagram scheduled 0, Instagram
  needs_approval 0, YouTube scheduled 0, YouTube needs_approval 0 — **total 0**, well
  under the cap of 8. Queue was not the blocker.
- Tooling check:
  - `node --version` → **v22.22.2** (20+ ✓)
  - `ffmpeg -version` / `ffprobe -version` → **missing**. Installed the BtbN static build
    per the brief (no apt-get): now `ffmpeg`/`ffprobe` **N-126734-ga9cbcc2bbb-20260921` ✓
  - `cd pipeline && npm ci` ✓
  - `npx playwright install chromium` → **failed**: the proxy answered **403 to CONNECT
    for `cdn.playwright.dev:443`** (organization egress policy). Not retried beyond the
    one repeat, per the proxy README.
  - Worked around the missing browser: the image ships Chromium 1194 at
    `/opt/pw-browsers`, so it was wired to the 1208 path the pipeline's
    playwright-core 1.58.2 expects. **Chromium then launched fine** — the browser was
    never the real blocker.

## Step 1 — pick the tool (passed)
Day 22 mod 6 = 4, so `am` = developer infra, and **`pm` takes category 5: writing/knowledge
tools**. Two Exa searches run as specified. Candidates with a vendor page inside 14 days:
Zettlr 4.8.0 (Sep 18), TypeFire 5.1 (Sep 15), HackMD Book Mode (Sep 16).

**Chosen: HackMD — Book Mode rebuilt.** Vendor `hackmd.io`. Not in `covered.json`, not
Anthropic, not waitlist-only, and the site shows the product publicly.

### The five facts (all vendor-owned pages)
1. HackMD is a collaborative Markdown workspace — teams write notes and docs together in
   real time, free tier included. — https://hackmd.io/pricing
2. Book Mode was rebuilt and the rebuild is driven from the sidebar; published
   **September 16, 2026**. — https://hackmd.io/blog/2026/09/16/improved-book-mode-2026
3. Where it lives: the book's **sidebar** — add an existing note, a new note, one from a
   template, or an external URL (it fetches the page title); drag to reorder; drop one item
   onto another to create a section, nested up to three levels; `# ` starts a section and
   `---` adds a divider. The renderer is a per-book setting under **Book settings → Book
   experience** (Neo or Classic). — same blog URL
4. Price: **Free, $0 forever**, unlimited notes, up to 3 teammates; **Prime $5 per
   seat/month** (billed annually) adds full-text search, PDF export, unlimited versions.
   — https://hackmd.io/pricing
5. Caveat: **editing a book's structure is desktop only in this release** (reading,
   navigating and sharing work on mobile). Also, once the sidebar writes back, paragraphs,
   tables, quotes and custom HTML/CSS in the book note are not carried through — HackMD
   saves a version-history checkpoint before that first write.
   — https://hackmd.io/blog/2026/09/16/improved-book-mode-2026

## Step 2 — write the lesson (blocked here)
The eight-step script was written (8 steps, **154 words**, inside the 150–170 range) and
kept below for the next run. The dry run then failed on the very first page load:

```
$ node scripts/capture.mjs --dry
page.goto: net::ERR_TUNNEL_CONNECTION_FAILED at
  https://hackmd.io/blog/2026/09/16/improved-book-mode-2026
    at tourGoto (pipeline/scripts/capture.mjs:95:14)
dry exit: 1
```

This is not a wrong `show` string and not a browser problem — it is the session's egress
policy. `curl -sI https://hackmd.io/pricing` returns `000`, and
`$HTTPS_PROXY/__agentproxy/status` records, for each attempt:

```
hackmd.io:443        gateway answered 403 to CONNECT (policy denial or upstream failure)
www.zettlr.com:443   gateway answered 403 to CONNECT (policy denial or upstream failure)
d1234.cloudfront.net:443  gateway answered 403 to CONNECT (policy denial or upstream failure)
cdn.playwright.dev:443    gateway answered 403 to CONNECT (policy denial or upstream failure)
```

Only GitHub, npm and Anthropic hosts are reachable. **Every** vendor site is denied, so
switching to Zettlr or TypeFire would fail identically — this is not a bad tool pick.
Per the proxy README, a 403 is an organization policy denial and must be reported rather
than retried or routed around.

The brief says: *"Do not continue until the dry run reports no miss."* So the run stopped
here, **before** `topview_generate_voice`. Nothing was spent and nothing was posted.

### Why step 5 (render on Actions) does not rescue this run
Step 5 requires pushing `lesson.json`, `caption.json` **and**
`assets/voice/narration.mp3`. Producing that mp3 means calling TopView and then
downloading the result URL — a CloudFront host, which the same policy denies (the
`d1234.cloudfront.net` probe above is the check). Spending the 0.1 credits would have
bought a file this session cannot fetch, so the spend was not made. The lesson directory
was also **not** committed: `.github/workflows/lesson.yml` triggers on
`lessons/*/lesson.json`, and pushing it without the narration would start a build that
cannot succeed.

## Buffer
No `create_post` calls. No posts created on either channel. `out/verify.json` never
existed, so the posting gate was never open.

## What a person should look at
The fix is one setting, not a code change: this environment's **network policy** has to
allow the vendor hosts the tour records (and the TopView/CloudFront download host).
Today's run had every other piece working — ffmpeg installed, Chromium launching, queue
empty, credit intact, five sourced facts, a script at 154 words. Re-run once egress is
opened and it should go straight through.

## Draft script, ready to reuse (HackMD, 8 steps, 154 words)
```json
{ "title": "Book Mode is a sidebar now",
  "mode": "tour", "viewport": {"width": 1440, "height": 810},
  "chrome": "hackmd.io",
  "hide": [],
  "steps": [
    {"id":"01","say":"Your team's best documentation is almost never in one note. It's in a dozen, and nobody wants to stitch them together.","page":"https://hackmd.io/blog/2026/09/16/improved-book-mode-2026","zoom":{"target":"wide"}},
    {"id":"02","say":"On September sixteenth, HackMD rebuilt Book Mode, and the whole thing now starts in the sidebar.","show":"Book Mode is rebuilt: build your book from the sidebar","zoom":{}},
    {"id":"03","say":"Everything you used to do by hand-editing a Markdown file happens there now.","show":"The sidebar is the book now","zoom":{}},
    {"id":"04","say":"Add an existing note, a new one, one from a template, or an external URL, and HackMD fetches the page title for you.","show":"The syntax was the problem","zoom":{}},
    {"id":"05","say":"Drag to reorder, drop one item onto another to make a section, and nest sections up to three levels deep.","show":"What changes depends on how you use books","zoom":{}},
    {"id":"06","say":"Prefer typing? A hash and a space starts a section, three dashes drops a divider, and every change writes straight back to the book.","show":"Open a book and look left","zoom":{}},
    {"id":"07","say":"It's on the free plan, zero dollars forever for up to three teammates, but editing a book's structure is desktop only in this release.","page":"https://hackmd.io/pricing","show":"Compare plans","zoom":{}},
    {"id":"08","say":"Open a book, look left, and try dragging two notes into one section.","zoom":{"target":"wide"}}
  ] }
```
Note: the `show` strings are taken from the crawled page headings but were **never
verified by a dry run**, so they must be re-checked on the next run.

`covered.json` was deliberately left unchanged — HackMD is still available to pick.
