# 2026-09-23 am — ABORTED at step 3 (0.1 credits spent, nothing posted)

**Abort reason:** the narration was generated successfully, but the session's egress policy
denies **every** TopView result host, so `assets/voice/narration.mp3` could not be
downloaded. Without that file the GitHub Actions renderer (step 5) has nothing to build,
so there is no `out/verify.json` and the posting gate never opened. Aborted **after** the
one TopView spend — the job itself cost exactly the capped 0.1 credits.

## Slot
- Run started 02:46 UTC → `am` slot. Voice **Kelly** `gVxI2bFMYAIstfKOTe8ZkAEbMcn1B6Ow`.
- Posts were due 03:30 UTC (09:00 IST). No posts were created.

## Step 0 — set up (passed)
- `git pull --rebase`: already up to date. `covered.json` holds one entry (the hand-made
  Claude demo, 2026-09-22) — no conflict with today's pick, and no vendor repeat.
- TopView credit **before: 2436.95**; **after: 2436.85** → **spend 0.10 credits** (at cap).
- Buffer queue (org `6aa7c7f3ac8fd4ea0a97166a`): Instagram scheduled 0, Instagram
  needs_approval 0, YouTube scheduled 0, YouTube needs_approval 0 — **total 0**, well under
  the cap of 8. Queue was not the blocker.
- Tooling check:
  - `node --version` → **v22.22.2** (20+ ✓)
  - `ffmpeg -version` / `ffprobe -version` → **missing**. Installed the BtbN static build
    per the brief (no apt-get): now **N-126756-g8b34c36e61-20260922** ✓ for both.
  - `cd pipeline && npm ci` ✓
  - No Playwright work attempted — the brief (step 0.4) says rendering happens on Actions.

## Step 1 — pick the tool (passed)
Day 23 mod 6 = 5, and `am` takes the day's category → **writing/knowledge tools**.
Two Exa searches run as specified. Candidates with a vendor page inside 14 days:
Zettlr 4.8.0 (Sep 18), HackMD Book Mode (Sep 16), TypeFire 5.1 (Sep 15), Fibery (Sep 17).

**Chosen: Zettlr — 4.8.0 adds admonitions.** Vendor `zettlr.com`. Not in `covered.json`,
not Anthropic, not waitlist-only, public docs and download pages show the product.

### The five facts (all vendor-owned pages)
1. Zettlr is a free, open-source Markdown writing tool — a "One-Stop Publication
   Workbench" for notes, citations, Zettelkasten/PKMS workflows and long-form writing.
   — https://docs.zettlr.com/en/
2. **Zettlr 4.8.0 was released September 18, 2026**, after almost three months of
   development; it adds admonition support, changes full-text search, and improves
   translations (including a new Azerbaijani one).
   — https://www.zettlr.com/post/zettlr-480-released
3. Where the change lives: admonitions use **blockquote syntax with a keyword header**,
   `> [!NOTE]`. Five types ship — `note`, `tip`, `info`, `warning`, `caution` — coloured
   grey, green, purple, yellow and red. Pre-rendering is **on by default** and is toggled
   in **preferences**, respecting the global raw/preview switch in the status bar. Custom
   titles and nesting are supported. — same URL
4. Price/tier: **"Zettlr is Free and Open Source Software (FOSS)"**, no paid tier, with
   direct downloads and package-manager installs for macOS, Windows and Linux.
   — https://www.zettlr.com/download
5. Caveat: the **full-text search behaviour changed** — a space between terms is now
   **AND**, not OR, so "boat ship" only matches files containing both. Restore the old
   behaviour with an explicit pipe (`|`). (Second caveat: Pandoc exports admonition
   containers **unstyled**, so export templates need manual CSS for now.)
   — https://www.zettlr.com/post/zettlr-480-released

## Step 2 — write the lesson (passed)
`lessons/2026-09-23-am-zettlr/` created; `lesson.json` and `caption.json` written.
**8 steps, 157 words** (inside 150–170). Title 31 chars. YouTube title 49 chars,
description 282 chars, IG caption 303 chars, 11 hashtags.

All five `show` strings were checked verbatim against the `web_fetch_exa` markdown for
their page before being used (the brief's no-browser check):

| step | show string | page | verbatim in crawl |
|---|---|---|---|
| 02 | `Zettlr 4.8.0 Released` | release post | ✓ (h1) |
| 03 | `Admonitions` | release post | ✓ (h2) |
| 05 | `pre-rendering of admonitions is turned on` | release post | ✓ (body) |
| 06 | `Change to Full-Text Search Behavior` | release post | ✓ (h2) |
| 07 | `Free and Open Source Software` | download | ✓ (body) |

Steps 01, 04 and 08 are `{"target": "wide"}` with no `show`, as the brief allows.
`capture.mjs` `tourFind` falls back to a non-exact `getByText`, so these are safe matches.

Hashtags: the Exa hashtag search returned no literal `#tags` on any top result, so tags
were taken from the terms that recur across those results instead — zettlr (189 mentions),
markdown (62), zettelkasten (43), pkm, note-taking apps, open source, academic writing.
Three generic tags at most, and all after the specific ones.

## Step 3 — narrate (spent, then blocked)
- `topview_generate_voice` — voice Kelly, `voiceSpeed: 1`, name `2026-09-23-am-zettlr`.
- **taskId `8264f561909d4440a7ba57ad665db947`** (boardTaskId
  `b27ccb223245431d84ae0c7fdb968a8d`), polled with `topview_query_task`
  (`taskType: "text_to_speech"`).
- `status: success` on the third poll, **`costCredit: 0.1`** — exactly the cap, correct.
- Audio duration reported **56.95 s**, inside the expected 40–70 s window.
- **The download failed.** Both result hosts are refused by the egress proxy:

```
api.topview.ai:443             gateway answered 403 to CONNECT (policy denial)
du9d8548ooqnc.cloudfront.net:443  gateway answered 403 to CONNECT (policy denial)
```

Tried, in order, all in-policy: the shortened URL (`api.topview.ai/s/...`), the
unshortened signed CloudFront URL (`shortenUrls: false`), and `topview_get_board_task` —
which returns a fresh signature on the **same** CloudFront host. No Topview MCP tool
returns the bytes themselves; `download_topview_canvas_nodes` is canvas-node only and this
was a standalone board task. Per the proxy README a 403 is an organization policy denial
that must be **reported, not retried or routed around**, so the run stopped here.

Host reachability probe from this session (`curl -sI`, HTTP status):

```
www.zettlr.com                 000
docs.zettlr.com                000
api.topview.ai                 000
du9d8548ooqnc.cloudfront.net   000
raw.githubusercontent.com      301
```

## Steps 4–7 — not reached
No local build (the brief routes rendering to Actions). The lesson directory was **not**
committed: `.github/workflows/lesson.yml` needs both `lesson.json` and
`assets/voice/narration.mp3`, so a push without the mp3 renders nothing. No frames to
look at. **No `create_post` calls — `out/verify.json` never existed, so the posting gate
was never open.** Buffer post ids: none.

## Step 8 — record
`covered.json` deliberately left **unchanged**, so Zettlr stays available to pick.

## What a person should look at
This is the **second consecutive run blocked by the same environment setting**, and the
blocker has now moved one step later and started costing money. On 22 Sep the vendor hosts
were denied, so the run aborted before spending. Today the brief correctly routes rendering
to GitHub Actions (which has open egress), so vendor-host denial no longer matters — but
**the TopView result host is still denied, and that denial now lands *after* the spend**.
Every future run will burn 0.1 credits and abort at the same line until this changes.

The fix is one setting, not a code change: add **`api.topview.ai`** and the TopView
CloudFront delivery host (**`*.cloudfront.net`**, currently `du9d8548ooqnc.cloudfront.net`
— the subdomain may rotate, so a wildcard is safer) to this environment's network policy
allowlist. Nothing else in the pipeline is broken: ffmpeg installed, queue empty, credit
intact, five sourced facts, a 157-word script, and all five `show` strings verified.

Until that lands, consider pausing the schedule so it stops spending 0.1 a run.

## Ready to reuse — `lesson.json` (Zettlr, 8 steps, 157 words)
```json
{
  "title": "Zettlr just added callout boxes",
  "mode": "tour",
  "viewport": { "width": 1440, "height": 810 },
  "chrome": "zettlr.com",
  "hide": [],
  "steps": [
    {"id":"01","say":"Your Markdown notes can finally highlight themselves. Zettlr, the free writing app built for notes and papers, just shipped callouts.","page":"https://www.zettlr.com/post/zettlr-480-released","zoom":{"target":"wide"}},
    {"id":"02","say":"Version four point eight arrived on September eighteenth, twenty twenty-six, after almost three months of development.","show":"Zettlr 4.8.0 Released","zoom":{}},
    {"id":"03","say":"The headline feature is admonitions. You might know them as callouts or alerts: boxes that wrap text and colour it by type.","show":"Admonitions","zoom":{}},
    {"id":"04","say":"There are five — note, tip, info, warning and caution — and you write one with plain blockquote syntax, bracket exclamation NOTE on the first line.","zoom":{"target":"wide"}},
    {"id":"05","say":"Pre-rendering is on by default, so the box draws itself once your cursor leaves it, and you can turn it off in preferences.","show":"pre-rendering of admonitions is turned on","zoom":{}},
    {"id":"06","say":"One change to watch: full-text search now treats a space as AND, not OR. Two words means a file needs both.","show":"Change to Full-Text Search Behavior","zoom":{}},
    {"id":"07","say":"Zettlr stays free and open source, on Mac, Windows and Linux, with no paid tier at all.","page":"https://www.zettlr.com/download","show":"Free and Open Source Software","zoom":{}},
    {"id":"08","say":"Open a note, type bracket exclamation TIP, and watch the box appear.","page":"https://docs.zettlr.com/en/","zoom":{"target":"wide"}}
  ]
}
```

## Ready to reuse — `caption.json`
```json
{
  "youtube_title": "Zettlr 4.8 adds admonitions: callouts in Markdown",
  "description": "Zettlr 4.8.0 (September 18, 2026) adds admonitions: note, tip, info, warning and caution callouts, written with blockquote syntax. Also in this release, full-text search now treats a space as AND, not OR. Free and open source. Source: https://www.zettlr.com/post/zettlr-480-released",
  "ig_caption": "Zettlr 4.8 finally lets your Markdown notes shout.\nFive callout types, written as > [!NOTE] — no plugin, no paid tier.\nHeads up: full-text search now treats a space as AND, not OR.\n\n#zettlr #zettelkasten #markdown #notetaking #pkm #opensource #academicwriting #writingtools #noteapps #productivity #tech",
  "first_comment": "Source: https://www.zettlr.com/post/zettlr-480-released",
  "hashtags": ["#zettlr","#zettelkasten","#markdown","#notetaking","#pkm","#opensource","#academicwriting","#writingtools","#noteapps","#productivity","#tech"],
  "sources": ["https://docs.zettlr.com/en/","https://www.zettlr.com/post/zettlr-480-released","https://www.zettlr.com/download"]
}
```

**Note:** the narration for exactly this script already exists in the TopView account
(board task `b27ccb223245431d84ae0c7fdb968a8d`, board `0128da7f281d40b4b9301c886f22cf34`,
Kelly, 56.95 s). If egress is opened it can be downloaded from the board without paying
again — so the next run should reuse it rather than spending a second 0.1.
